# Zeus IoT static site server — environment variables
# (The site is a static WhatsApp funnel; there is no database or auth.)

# Port the server listens on. Render sets this automatically.
PORT=3000

# Environment
NODE_ENV=development
